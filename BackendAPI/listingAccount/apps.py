from django.apps import AppConfig

# Used to include application configuration for the app LEAVE DEFAULT
class ListingAccountConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'listingAccount'